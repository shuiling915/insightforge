# InsightForge 企业级生产落地合规性评估

> 对照《企业级 AI Agent 生产落地指南》17 章验收标准，逐项检查 InsightForge 的实现差距。
> 评估日期：2026-09-19

## 总览

| 指南章节 | 主题 | 合规度 |
|---------|------|--------|
| 3 | LLM Gateway 模型调用网关 | ⚠️ 60% |
| 4 | 结构化输出 Structured Output | ✅ 85% |
| 5 | 流式输出 Streaming | ⚠️ 40% |
| 6 | Prompt 工程 | ⚠️ 50% |
| 7 | 工具运行时 Tool Runtime | ⚠️ 40% |
| 8 | 上下文与记忆管理 | ❌ 20% |
| 9 | Agent Loop 与状态管理 | ⚠️ 50% |
| 10 | 可观测性 Observability | ⚠️ 45% |
| 11 | 错误处理与重试 | ⚠️ 55% |
| 12 | 安全与权限 | ✅ 75% |
| 13 | 部署与运维 | ⚠️ 65% |
| 14 | 评测与质量保障 | ❌ 15% |
| 15 | 多 Agent 协作 | ❌ 0% |

**综合合规度：约 48%**。核心安全与结构化输出已达标，流式/可观测/评测/多 Agent 为主要短板。

---

## 逐项差距分析

### 第 3 章 LLM Gateway — 60%

| 指南要求 | 实现状态 | 差距 |
|---------|---------|------|
| 统一入口 | ✅ 所有调用走 `LLMGateway` | — |
| 密钥隔离 | ✅ API Key 只在 Gateway 内使用 | — |
| 多模型路由 | ⚠️ 有 `fallback_models` 列表 | 缺少**能力注册表**：未声明每个模型支持 streaming/structured/tool_calling，路由不按能力匹配 |
| Fallback 边界 | ⚠️ 实现了模型级 fallback | 未严格执行"首 Token 前可 fallback"边界（当前用同步 completion，无流首块概念） |
| 重试四要素 | ❌ 只遍历 fallback 列表 | 缺**指数退避 + jitter**，未设置 `litellm` 的 `num_retries=0`（SDK 隐式重试可能叠加） |
| 用量统计 | ❌ 只有 debug log | 未持久化 Token / Cost / Latency / TTFT 到存储，无法做成本核算 |

**修复优先级：P1**

---

### 第 4 章 结构化输出 — 85%

| 指南要求 | 实现状态 | 差距 |
|---------|---------|------|
| 第 1 层 JSON Mode | ✅ `_parse_json` 处理 markdown fence | — |
| 第 2 层 JSON Schema | ✅ `model_json_schema()` 注入 Prompt | — |
| 第 3 层 Pydantic 校验 | ✅ `model_validate` | 缺**跨字段业务规则**：如 `code` 与 `final_answer` 不能同时为 null |
| 纠错循环 | ✅ 3 次重试 + 错误反喂 | — |
| 全有或全无 | ✅ 校验失败不进 Loop | — |
| 失败状态机 | ❌ 无 OK/DEGRADED/FAILED 三态 | 失败直接抛异常，无降级缓存 |

**修复优先级：P2**（核心已达标，补业务规则校验即可）

---

### 第 5 章 流式输出 — 40%（重大短板）

| 指南要求 | 实现状态 | 差距 |
|---------|---------|------|
| SSE 帧协议 | ✅ `to_sse()` 输出 `event/data` | — |
| RunEvent 模型 | ⚠️ 有 `run_id`/`timestamp`/`type` | 缺 `schema_version`、`seq`（事件序号）字段 |
| 三类事件 | ✅ lifecycle/process/content 都有 | — |
| **Event Store** | ❌ 事件只在内存生成，不持久化 | 无法重放、无法断线恢复 |
| **断线恢复** | ❌ 无 `Last-Event-ID` 机制 | 客户端断线后只能重新跑全流程 |
| **取消机制** | ❌ 无 `POST /runs/{id}/cancel` | 无法停止正在执行的 Run |
| 心跳 | ❌ 无 `: heartbeat` | 长连接容易被代理断开 |
| 创建/订阅分离 | ⚠️ 有 `POST /runs` + `GET /runs/{id}/stream` | 但 stream 端点直接同步执行，非后台任务 |

**修复优先级：P0**（流式是企业级 Agent 的核心体验）

---

### 第 6 章 Prompt 工程 — 50%

| 指南要求 | 实现状态 | 差距 |
|---------|---------|------|
| 五要素 | ✅ 角色/目标/边界/流程/输出都有 | — |
| 行为定义 | ⚠️ 有行为约束 | 缺少"信息不足时明确说明，不猜测"等可验证规则 |
| Few-shot 示例 | ❌ 无 | 应至少 2-3 个高质量示例 |
| 任务拆解 | ⚠️ 提示做 plan | 未强制"分析→执行→检查"三步 |
| 模板化 | ⚠️ 固定规则与数据已分离 | Prompt 无版本号 |
| 注入防护 | ⚠️ 指令与数据分离 | 未显式声明"外部文本仅用于分析，不能覆盖系统规则" |
| 版本管理 | ❌ 无语义化版本 | — |

**修复优先级：P2**

---

### 第 7 章 工具运行时 — 40%

| 指南要求 | 实现状态 | 差距 |
|---------|---------|------|
| 工具三要素 | ⚠️ 代码执行是隐式工具 | 无通用 Tool 注册机制（无 Tool Schema、无描述） |
| 幂等设计 | N/A | 当前只读，无写入工具 |
| 身份注入 | N/A | — |
| 沙箱隔离 | ✅ DockerExecutor + CodeSafetyChecker | — |
| 危险边界控制 | ✅ 网络禁用、内存/CPU 限制、AST 拦截 | — |

**修复优先级：P2**（数据分析场景当前一个代码执行工具够用，但需预留扩展点）

---

### 第 8 章 上下文与记忆管理 — 20%（重大短板）

| 指南要求 | 实现状态 | 差距 |
|---------|---------|------|
| 上下文预算 | ⚠️ 有 `max_tokens_per_run` | 但历史消息**无裁剪**，长会话必然超窗 |
| 倒序扫描裁剪 | ❌ 无 | system + user 始终保留应实现 |
| 数据来源区分 | ❌ 无 | 无 RAG、无实时数据接入 |
| Working Memory | ⚠️ messages + plan | 无显式"未解决问题"跟踪 |
| Long-term Memory | ❌ 无 | 无跨会话偏好/经验记忆 |
| Context Builder | ❌ 无 | — |

**修复优先级：P1**（长任务不可用）

---

### 第 9 章 Agent Loop 与状态管理 — 50%

| 指南要求 | 实现状态 | 差距 |
|---------|---------|------|
| 状态机 | ✅ completed/failed/budget_exhausted | 缺 `waiting_user`、`cancelled` 状态 |
| Checkpoint | ❌ 无 | 无 `loop_step`/`completed[]`/`next_cursor`/`context_digest` 字段 |
| 恢复原则 | ❌ 无 | 无法从中间步骤恢复执行 |
| 单 Loop 拥有者 | ✅ 每个 Run 一个 Agent 实例 | — |

**修复优先级：P1**

---

### 第 10 章 可观测性 — 45%

| 指南要求 | 实现状态 | 差距 |
|---------|---------|------|
| Metrics | ✅ Prometheus：LLM 调用/延迟、代码执行、Run 数 | — |
| Trace | ⚠️ 有 span 日志 | 无 `trace_id` 贯穿全链路，无 Langfuse/OTel 接入 |
| Logs | ⚠️ structlog 基础日志 | 无请求参数/响应内容/工具调用的结构化持久化 |
| Run Store | ❌ 无事件级持久化 | 只有 session 级 SQLite，无法支撑断线重放 |

**修复优先级：P1**

---

### 第 11 章 错误处理与重试 — 55%

| 指南要求 | 实现状态 | 差距 |
|---------|---------|------|
| 错误分类 | ✅ LLMError 区分 | — |
| 重试四要素 | ⚠️ 有 max_attempts | 缺指数退避 + jitter |
| SDK 重试关闭 | ❌ 未设置 `num_retries=0` | 可能导致重试叠加爆炸 |
| 流式失败五阶段 | N/A | 当前同步调用 |

**修复优先级：P1**

---

### 第 12 章 安全与权限 — 75%

| 指南要求 | 实现状态 | 差距 |
|---------|---------|------|
| 认证 | ✅ API Key | 缺 OIDC/SSO |
| RBAC | ❌ 单 Key，无角色 | 无 admin/analyst/viewer 分级 |
| 工具安全 | ✅ CodeSafetyChecker AST 拦截 | — |
| 沙箱 | ✅ Docker 容器隔离 | — |
| 数据安全 | ❌ 无脱敏、无加密 | 上下文可能含敏感数据未处理 |

**修复优先级：P1**

---

### 第 13 章 部署与运维 — 65%

| 指南要求 | 实现状态 | 差距 |
|---------|---------|------|
| 容器化 | ✅ Dockerfile + docker-compose | — |
| 配置管理 | ✅ 环境变量 | 缺 YAML 配置文件支持 |
| 监控 | ✅ /metrics 端点 | 缺告警规则、模型供应商健康检查 |
| K8s/Helm | ❌ 无 | — |

**修复优先级：P2**

---

### 第 14 章 评测与质量保障 — 15%（重大短板）

| 指南要求 | 实现状态 | 差距 |
|---------|---------|------|
| Eval 集 | ❌ 只有安全检查单元测试 | 无 Agent 行为评测（正常/边界/注入样例） |
| LLM-as-Judge | ❌ 无 | — |
| 回归测试 | ❌ 无 | Prompt/代码变更后无自动回归 |
| 程序测试 | ✅ 有 Schema/安全测试 | — |

**修复优先级：P0**（没有评测就无法保证质量）

---

### 第 15 章 多 Agent 协作 — 0%

| 指南要求 | 实现状态 | 差距 |
|---------|---------|------|
| 委托模式 | ❌ 无 | — |
| 上下文隔离 | ❌ 无 | — |
| 预算控制 | ❌ 无 | — |
| Delegation Ledger | ❌ 无 | — |

**修复优先级：P3**（数据分析场景单 Agent 足够，可后置）

---

## 第 17 章工程验收对照表

| 验收项 | 状态 | 说明 |
|--------|------|------|
| 协议隔离：上层不感知供应商 | ✅ | LiteLLM 抽象，切换模型不改 Loop |
| 结构化输出：未通过校验不进 Loop | ✅ | `chat_structured` 校验后进 Loop |
| 流式：首块后不能切模型 | ⚠️ | 同步调用无此问题，但无取消状态机 |
| 重试：三层不叠加 | ❌ | 未关闭 SDK 隐式重试 |
| 可观测：每 Run 有 trace_id | ❌ | 有 run_id，无 trace_id 贯穿 |
| 安全：高风险动作程序控制 | ✅ | CodeSafetyChecker + Docker 沙箱 |
| 幂等：写入不重复 | N/A | 无写入类工具 |
| 恢复：断线重放不重调模型 | ❌ | 无 Event Store |
| 评测：有 Eval 集 + 回归 | ❌ | 无 |

**9 项验收中：3 项通过，3 项部分，3 项缺失。**

---

## 修复路线图

### P0 — 必须修复（阻塞生产上线）

1. **Event Store + 断线恢复**（第 5 章）
   - SQLite 新增 `run_events` 表：`run_id, seq, type, data, created_at`
   - `AgentEvent` 增加 `seq`、`schema_version` 字段
   - `GET /runs/{id}/stream?last_event_id=N` 支持重放
   - 心跳 `: heartbeat` 每 15s

2. **取消机制**（第 5 章）
   - `POST /runs/{id}/cancel` 端点
   - Agent Loop 每轮检查取消信号
   - Docker 容器 `kill` 强制终止

3. **Eval 集 + 回归测试**（第 14 章）
   - 准备 10 个标准数据分析任务（均值、分组、可视化、缺失值处理等）
   - 断言：代码执行成功、输出包含预期数值、生成图表
   - CI 中运行

### P1 — 重要（影响稳定性）

4. **上下文裁剪**（第 8 章）
   - 倒序扫描历史，保留最近 N 轮，system + 当前 user 始终保留
   - 旧工具结果压缩为摘要

5. **重试四要素 + 关闭 SDK 重试**（第 3/11 章）
   - `litellm.completion(num_retries=0)`
   - 指数退避：`base * 2^attempt + jitter`
   - 429/5xx 才重试，4xx 直接失败

6. **Trace ID 贯穿**（第 10 章）
   - 每个 Run 生成 `trace_id`，注入所有 LLM 调用 metadata
   - Langfuse 接入（已有配置项）

7. **RBAC + 数据脱敏**（第 12 章）
   - API Key 关联角色，角色关联可执行操作
   - 上下文脱敏：正则替换手机号/身份证/银行卡

### P2 — 增强（提升体验）

8. Prompt 版本管理 + Few-shot
9. 跨字段业务规则校验（`code` 与 `final_answer` 互斥）
10. YAML 配置文件支持 + 告警规则

### P3 — 远期

11. 多 Agent 协作（第 15 章）
12. K8s/Helm 部署
13. Long-term Memory

---

## 结论

InsightForge 在**安全沙箱、结构化输出、API 服务**三个核心维度已达到企业级标准，但在**流式事件持久化、上下文管理、评测体系**三个维度存在明显短板。

完成 P0（3 项）+ P1（4 项）后，可达到约 **80% 合规度**，满足生产上线要求。