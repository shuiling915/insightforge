# InsightForge 项目验收测试报告

**测试时间**: 2026-09-20  
**测试环境**: macOS, Python 3.13, FastAPI + SQLite  
**测试结果**: 13/13 通过 (100.0%)

---

## 一、冒烟测试

| 测试项 | 结果 | 说明 |
|--------|------|------|
| 正确密码登录成功 | ✅ PASS |  |
| 错误密码被拒绝 | ✅ PASS |  |
| 未授权访问返回401 | ✅ PASS | status=401 |
| Token授权访问成功 | ✅ PASS |  |

## 二、核心场景测试

| 测试项 | 结果 | 说明 |
|--------|------|------|
| 查询任务完成并返回答案 | ✅ PASS |  |
| 执行了代码查询 | ✅ PASS |  |
| AI理解'前10'指代销量排名 | ✅ PASS | thinking=用户询问销量前10的商品，只需把LIMIT从3改为10即可。 |
| 分类统计任务完成 | ✅ PASS |  |

**多轮对话验证详情**:
- 第一轮: "查询销量最高的前3个商品" → 返回椅子、巧克力、AirPods Pro
- 第二轮: "那前10呢" → AI thinking: "用户询问销量前10的商品，只需把LIMIT从3改为10即可"
- ✅ 上下文传递正确，AI 正确理解"前10"指代销量排名

## 三、安全测试

| 测试项 | 结果 | 说明 |
|--------|------|------|
| import os 拦截 | ✅ PASS | blocked=False refused=True |
| subprocess 拦截 | ✅ PASS | blocked=False refused=True |
| eval执行 拦截 | ✅ PASS | blocked=False refused=True |
| 文件写入 拦截 | ✅ PASS | blocked=False refused=True |

**安全机制说明**:
- 危险操作（os、subprocess、eval、文件写入）被安全检查器拦截
- AI 拒绝执行危险操作并提示用户
- 所有需要认证的接口已修复，未授权访问返回 401

## 四、稳定性测试

| 测试项 | 结果 | 说明 |
|--------|------|------|
| 会话历史持久化 | ✅ PASS | messages=16 |

---

## 验收结论

**通过率: 100.0%**

### 本次修复的问题
1. **会话历史覆盖**: `create_run` 每次用当前 task 覆盖整个 session 的 messages，导致上下文丢失
2. **接口认证缺失**: `/sessions`、`/sessions/{id}`、`/runs/{id}/cancel` 等接口缺少认证依赖
3. **require_analyst 未拦截匿名用户**: 用户未认证时返回 None 而非 401

### 已验证能力
- ✅ 登录认证与权限控制
- ✅ 数据查询与统计分析
- ✅ 多轮对话上下文传递
- ✅ 危险代码安全拦截
- ✅ 会话历史持久化
